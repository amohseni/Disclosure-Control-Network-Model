###############################################################
# RUN SHINY APP :: Selective Disclosure & Perceptions of Polarization
###############################################################

# Open app in browser
options(shiny.launch.browser = TRUE)
shinyApp(ui = ui, server = server)